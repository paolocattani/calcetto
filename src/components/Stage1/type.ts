export type Pair = {
  pairAlias: string;
  stage1Name: string;
  placement: number;
  paid: boolean;
  tournamentId: number;
  player1Id: number;
  player2Id: number;
};

export type wapperPropsType = {};

export type headerPropsType = {
  title: string;
};
